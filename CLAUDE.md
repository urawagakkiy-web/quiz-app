# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) が quiz-app を扱うときのガイダンスです。

## プロジェクト概要

**quiz-app** — 一般常識クイズアプリ。
4択形式で一般常識（歴史・地理・科学・時事など）を出題し、最後に得点と解説を表示する。

- 技術スタック: **HTML / CSS / JavaScript のみ**
- ビルドツール・パッケージマネージャ・フレームワークは使わない
- **外部依存なし**。`index.html` をダブルクリックしてブラウザで開ければ完成

## 言語

**ユーザーへの返答・UI・解説文・コメント・README・コミットメッセージは、すべて日本語で書く。**

## ファイル構成

```
quiz-app/
├── index.html   # 骨組みだけ（ヘッダー・進捗バー・#main の空コンテナ）
├── style.css    # 見た目。テーマ色は :root のCSS変数に集約
├── script.js    # QUIZ データと画面遷移
└── CLAUDE.md
```

この3ファイル構成を保つ。相対パス（`./style.css` / `./script.js`）で読み込むので、`file://` で開いても動く。
`index.html` に直接 `<style>` / `<script>` を書き足さず、それぞれのファイルに置くこと。

## コマンド

```bash
# アプリを開く
open quiz-app/index.html
```

ビルド・インストール・テスト実行のコマンドは存在しない。

## 実装の方針

### 画面遷移

`renderStart()` → `renderQuestion()` → `select()` → `finish()` の単純な状態機械にする。
ルーターは作らず、各関数が `main.innerHTML` を丸ごと差し替え、直後に `onclick` を貼り直す。
進行状態は `index` / `score` などモジュールスコープの変数が持つ。

### 問題データ

```js
const QUIZ = [{ q, choices: [4つ], answer: 0-3, note }]
```

**問題数はどこにもハードコードしない。** 「全◯問」・進捗バー・成績判定は、すべて `QUIZ.length` と正答率（`rate`）から算出する。
`score >= 4` のような得点の固定値で判定を書かないこと。`QUIZ` を差し替えるだけで他が追従する状態を保つ。

### 保存まわり

履歴や設定を保存する場合、**`localStorage` を直接呼ばない。必ずラッパー（`store`）経由で読み書きする。**
`file://` で開いたときに `localStorage` が `SecurityError` を投げ、直接呼ぶと初期描画ごと落ちるため。
ラッパーは失敗時にメモリへ退避し、保存できていないことを画面に警告として出す。

### 配色

`:root` の CSS 変数だけでテーマが決まるようにする。個々のセレクタに色を直書きしない。

### アクセシビリティ・表示

- スマホ幅（375px）で横スクロールが出ないこと
- 選択肢はタップしやすい大きさにする
- 正解／不正解は色だけでなく記号（○×）でも示す

## 作問のルール

- 一般常識として**答えを知らなくても納得できる**レベルにする。マニアックな知識は避ける
- 正解の選択肢が 0〜3 に偏らないようにする
- 誤答は同じジャンル・同じ粒度から選び、**消去法で消せるものにしない**
- 解説（`note`）には正解の根拠＋関連する豆知識を1〜2文で添える
- **時期で変わる事実は出題しない**（現職の役職者、ランキング、最新記録など）。変わらない事実を選ぶ
- 実在の人物・団体を扱う場合は**裏取りしてから作問する**

## 動作確認

テストはないので、**ブラウザで実際に開いて通しで解く**ことで確認する。
ブラウザペインから JS を実行し、`QUIZ[i].answer` を順に click して満点になるかを見るのが早い。
**「ファイルを書いた」で終わらせない。**
